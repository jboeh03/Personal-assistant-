User-agent: *
Allow: /

# thanks.html, membership-full.html and 404.html are kept out of the index with
# <meta name="robots" content="noindex"> and NOT with Disallow. The two are not
# interchangeable: a Disallowed page is never crawled, so the noindex on it is
# never read, and the bare URL can still surface as a result with no title and
# no description. membership-full.html is a near-duplicate of the home page, so
# blocking it is the one way to guarantee it shows up looking unexplained.
# Leave this file allowing everything.

Sitemap: [URL]/sitemap.xml
